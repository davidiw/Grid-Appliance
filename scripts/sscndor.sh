#!/bin/bash
#In here goes some some site specific changes to the condor_config
#make sure if you want to add class ad values to start STARTD_EXPRS
#to do it like this:
#echo "CONDITION = VALUE" >> /etc/condor/condor_config
#echo "STARTD_EXPRS = STARTD_EXPRS, CONDITION" >> /etc/condor/condor_config
